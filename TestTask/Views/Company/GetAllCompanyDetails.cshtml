@model IEnumerable<TestTask.Models.Company>

<p>
    @Html.ActionLink("Create New", "AddCompany")
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Size)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Form)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Size)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Form)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditCompanyDetails", new { id=item.CompanyId }) |
            @Html.ActionLink("Delete", "DeleteCompany", new { id=item.CompanyId })
        </td>
    </tr>
}

</table>
